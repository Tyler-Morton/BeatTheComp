.env
__pycache__/
*.pyc
.DS_Store

# Standalone research / shadow tester — keep local, do not push
backtest_lowvol.py
shadow_backtest.py
lowvol_prices_cache.parquet
shadow_strategy_log.csv
strategy_prices_cache.parquet
strategy.py
strategy_lab.py
strategy_lab_cache.parquet
ROADMAP.md
numerai/
kaggle/

# Local Claude Code settings (machine-specific, do not push)
.claude/settings.local.json

# Challenger bot - code is tracked (validated 2026-06-09, runs via Actions);
# only the big research backtest cache stays local
challenger/prices_cache.parquet

# Research lab - local only, never pushed (experiments + journal)
research/

# Python course - personal learning scratch, not part of the project
learn/

# Working agreement / operating notes for Claude Code - local only
CLAUDE.md

# Write-only artifacts: generated each run, read by nothing (keep local)
trending_raw.csv
alerts.log
backtest_results.png
perf_report.png
tracker.png
vs_spy.png

# Regenerable price cache (same treatment as the other *_cache.parquet files)
perf_prices.parquet

# local backups — must never reach the repo, and volbot.yml does `git add -A -- volbot/`
*.bak
